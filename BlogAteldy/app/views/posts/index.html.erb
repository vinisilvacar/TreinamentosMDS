<h1>Listando Posts</h1>
<%= link_to 'Novo Post', new_post_path %>
  <table>
    <tr>
      <th>Título</th>
      <th>Texto</th>
    </tr>
    <% @posts.each do |post| %>
      <tr>
        <td><%= post.title %></td>
        <td><%= post.text %></td>
        <td><%= link_to 'Mostrar', post_path(post) %></td>
      </td>
    <% end %>
  </table>
